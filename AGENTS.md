# AGENTS.md

## Cursor Cloud specific instructions

### Repository State

This repository currently contains only a project brief `README.md` for a "Parking" web application (sponsored by Skillup Labs). There is **no application code, no dependencies, no services, and no build/test/lint configuration**.

### Development Environment

- **No dependency installation** is required — there are no `package.json`, `requirements.txt`, or similar files.
- **No services** to start — there is no backend, frontend, or database configured.
- **No lint/test/build commands** exist yet.

### When Code Is Added

Once application code is scaffolded, update this file and the VM update script accordingly. The README suggests a tech stack of:
- Modern JavaScript framework (frontend)
- Server-side framework supporting APIs (backend)
- Relational database system

Typical setup after scaffolding would involve `npm install` (or equivalent) and starting dev servers per `package.json` scripts.
